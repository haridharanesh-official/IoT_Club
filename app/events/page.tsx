import { UnavailableFeature } from '@/components/public/UnavailableFeature'

export default function Page() {
  return <UnavailableFeature title="Events and registration" detail="No public event registration or attendance workflow is available until it is backed by current event records." />
}
