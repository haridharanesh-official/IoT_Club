import { UnavailableFeature } from '@/components/public/UnavailableFeature'

export default function Page() {
  return <UnavailableFeature title="Team applications" detail="Team applications are not open because no database-backed application workflow is connected." />
}
