import { UnavailableFeature } from '@/components/public/UnavailableFeature'

export default function Page() {
  return <UnavailableFeature title="Teacher evaluations" detail="Evaluation actions are unavailable until persistent assignments and authorized review records are connected." />
}
